# 变更记录

## v1.5.0

- 优化首页字形 Canvas：限制高像素密度设备的渲染倍率，以 60 帧为绘制上限，页面离开视口时暂停，恢复时复用已有画布；窗口尺寸变化改为延迟合并处理，深浅色切换只更新绘制颜色，减少滚动和切换页面时的持续开销。
- 明确首页文章排序：首页首屏与“最新文章”按 `date` 倒序取最新内容；“精选文章”优先显示 `featured = true` 的前三篇，没有显式精选时回退到最新三篇；最新区域不再错误跳过精选区域中的文章。
- 升级 `/posts/` 文章列表搜索，索引从标题和正文片段扩展到标题、完整正文、摘要、分类与标签；结果按字段相关度排序，同分时优先较新的文章，多个关键词需全部命中，最多显示十条。
- 改进中文搜索体验：中文单字即可查询，纯英文或数字保持至少两个字符；补充加载中、无结果、索引失败和过短查询提示。
- 搜索时以结果替换原分页列表，支持清除按钮和 `Esc` 恢复；增加安全高亮、同源链接校验、日期与分类/标签元数据，以及输入框和动态结果的无障碍状态。
- 完善搜索的移动端适配，调整输入框、触控按钮、结果卡片、字号和元数据密度，保证 320px 窄屏下不出现横向溢出。
- 更新 README、配置部署手册和主题介绍文章，补充 `featured`、首页性能策略、搜索范围、排序逻辑、移动端行为与 `v1.5.0` 升级示例。
- 扩充浏览器回归，覆盖首页最新文章顺序、搜索加载竞态、字段相关度、中文单字、列表恢复和窄屏布局。

## v1.4.9

- 统一全站字体职责：界面、导航与标题使用自托管 Inter，文章正文使用自托管 Noto Serif SC，代码继续使用等宽字体栈。
- 将 Noto Serif SC 的 400、700 字重按核心、常用和扩展字符拆分，文章页只预加载核心正文子集，其余字符按需加载，减少首次阅读的字体传输量。
- 为正文设置与本地衬线字体指标接近的系统回退，禁止正文合成不存在的字重和斜体，并避免字体迟到后替换已显示文字。
- 字体纳入 Hugo Pipes，由模板注入带内容指纹的资源地址；主题仍输出一个主样式文件，普通用户无需运行额外构建工具。
- 修正 Giscus iframe 字体回退声明，补充字体子集生成脚本、产物校验和浏览器回归测试。
- 隔离字体回归测试中的 About 访客接口，避免外部服务限流导致 CI 随机失败。
- 更新字体来源、授权、维护方法、目录结构及示例站安装说明。

## v1.4.8

- 统一包括单篇文章页在内的桌面端 Dock 半透明表面。

## v1.4.7

- 修复根路径及子目录部署下分类、标签和文章页导航的选中状态，并增加服务端输出和浏览器视觉回归。
- 合并目录重复的移动端媒体查询监听，统一移动 Dock 的半透明背景变量。
- 减少 CI 中重复的严格构建、产物验证和非浏览器矩阵依赖安装。
- 删除未参与 Hugo Pipes 构建的空 CSS 入口文件。

## v1.4.6

- 严格检查 Hugo 构建警告、搜索索引、分类和标签详情页，并在 CI 中覆盖最低版本与较新版本 Hugo。
- 补充搜索、Giscus 模板与主题同步、About 四级访客请求顺序、超时、失败显示与缓存、目录无障碍状态、Dock 恢复和 404 游戏等浏览器回归。
- 修复搜索索引加载期间的查询竞态，统一脚本的 ES2018 兼容范围。
- 为移动端目录增加明确的展开状态，减少关键布局对高级选择器的依赖。
- 降低移动端及文章页 Dock、移动菜单、评论和目录按钮的背景不透明度，并允许小型导航菜单展开时继续滚动页面。
- 为示例站增加明确标注且不可提交的评论区模拟展示；该模式默认关闭，不影响真实 Giscus。
- 按职责继续拆分文章阅读、导航和响应式样式，保持单一 CSS 与 JavaScript 产物。
- 默认图片缓存版本改为根据文件内容自动生成。
- 固定 GitHub Actions 依赖版本并启用自动更新检查。
- 为首页、长文章和 About 页增加浅色、深色视觉基线比较，同时保留 CI 截图产物。
- 扩展默认深色 Logo、深色 favicon 和文章默认封面的内容哈希验证。
- 明确浏览器功能基线、Chromium 自动回归范围、本地严格构建方式和安全问题私密报告流程。

## v1.4.5

- 移动端目录与评论按钮改为纵向排列，短目录从底部展开，长目录保持视口内滚动。

## v1.4.4

- 将目录和评论按钮内嵌到移动端目录窗口。

## v1.4.3

统一全站界面字体名称，确保优先加载自托管 Inter。

将文章正文链接和粗体明确映射到 Noto Serif SC 已提供的 700 字重。

## v1.4.2

修复移动端短目录仍占满侧栏高度、遮挡正文的问题。


## v1.4.1

统一文章页和 About 页的短代码颜色与样式。

更新了默认文章封面

默认文章封面和 README 页面预览改用 WebP，减少图片体积。

修复图片渲染钩子在 Hugo 0.112 下无法构建的问题。

## v1.4.0

- 按功能拆分 JavaScript 和 CSS，保留现有页面、动画、响应式布局及交互行为。
- 使用 Hugo Pipes 合并、压缩并指纹化主题资源，浏览器仍只加载一个主样式和一个主脚本。
- 增加 GitHub Actions 和 Playwright 回归测试，覆盖主要页面、主题模式、导航、Dock、目录及五种常用视口。
- 完善示例站构建检查、长文章回归页面和本地测试命令。
- 统一 README、示例配置、Hugo Module 和 Go 版本要求，并补充 About 页访客信息说明。
- 补充 Inter 与 Noto Serif SC 的来源、署名和 SIL Open Font License 1.1 文本。

## v1.3.4

1. 保留文章特色字体，TTF 无损转为 WOFF2。字形、字符映射完全一致，体积从 29.56 MB 降至 11.57 MB，减少约 60.9%
2. 首页 Canvas 改为时间驱动，适配 60/120Hz；缓存颜色与渐变，预计算字符行样式，移除每帧遮罩和多余对象分配。
3. 长文章目录改为坐标缓存加二分查找，只在字体、图片或尺寸变化时重新测量。
4. Dock 已删除时间与 IP 地区功能；移动端保持 68–72px 厚度，320px 下也没有重叠。
5. About 页 IP 和位置完全独立，只进入 About 时请求，并加入会话缓存、超时和正则安全处理。
6. 文章列表和分类/标签页加入响应式分页。
7. 修复空目录仍显示侧栏的问题，同时避免发布日期与更新时间重复。
8. Markdown 页面包图片自动生成四档 WebP，保留原图回退，并启用原生懒加载。
9. 修复“减少动态效果”模式下 Dock 合并状态与视觉状态不一致。
10. 删除失效的阅读进度监听、旧图片观察器、废弃 Dock 类和从未触发的事件监听。
11. PC 三合一状态下，品牌、菜单、模式区统一使用 `999px` 胶囊圆角，左右端视觉一致
12. 移动端深浅模式均去除内部底色、边框、阴影和高光，只保留最外层 Dock 胶囊轮廓

## v1.3.3

修复了不同尺寸下dock合并后偏右的问题

about模版页面底部联系方式栏目和页脚重复，修改为当前IP和位置信息

## v1.3.2

更新：所有桌面页面顶部都为三块分立 Dock，向下滚动时连续靠拢、外壳展开、内部轮廓淡出，最终三合一，向上滚动时按照同一进度反向分离，只有首页保留顶部磁吸，其他页面不会自动拉动页面，文章页融合过程保持透明材质，正文仍能透过 Dock，手机端继续使用紧凑菜单，不执行桌面融合动画。

## v1.3.1

1、分散的版本号统一收拢，现在版本号只在一个地方维护：theme-asset-version.html，新增了一个统一处理入口：theme-asset-url.html，它负责判断：如果资源是主题内置默认资源，就自动加 `?v=...`；如果是用户自定义资源，就只正常解析 URL，不加版本号。已接入这些默认资源：

- 默认 logo / 深色 logo
- 默认 favicon / 深色 favicon
- 默认头像
- 默认文章封面
- 默认 OG 图

2、清掉了之前散落在这些文件里的版本号逻辑：

* header.html
* baseof.html
* index.html
* list.html
* about.html

3、重构三合一 Dock 的交互结构：合并态使用统一外壳，三个内容区只负责位置变化，背景和轮廓由共享 Dock 呈现。

## v1.3.0

重做了一下logo，让主干更长，分支更靠内，就是更像雪花

扁平化了药丸标签

重做了dock融合逻辑

## v1.2.x

该系列版本主要包含样式、交互和兼容性调整；具体内容以对应 Release 说明为准。

## v1.2.2

- 优化首页 Dock 动效、透明导航和多尺寸布局。
- 优化文章页排版、目录样式和长文目录跟随。
- 代码块调整为 macOS / GitHub 风格，强化移动端适配。
- 新增 404 页面和接雪花小游戏。
- 美化 Giscus 评论区，新增 IMX 浅色 / 深色评论主题。
- 更新 README 中的主题配置说明。

## v1.x.x

该系列版本的详细记录见对应 Release 说明。

## v1.0.0 - 2026-06-25

第一个正式版。

- 修复已知问题。
